# 463. Island Perimeter


class Solution(object):
    def islandPerimeter(self, grid):
        """
        :type grid: List[List[int]]
        :rtype: int
        """
        if ()
        topLayer = []
        targetLayer = []
        botLayer = []

        numOfLayer = len(grid)
        i = 0
        while (i < numOfLayer):
            if (topLayer == []):
                topLayer = grid[i]
                continue
            if (targetLayer == []):
                targetLayer = grid[i]
                continue

            i += 1


def numOfLandSurrending(grid, target):
    numOfLandSurrending = 0

    return numOfLandSurrending
